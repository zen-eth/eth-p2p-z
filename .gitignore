.zig-cache/
zig-pkg/
zig-out/
build/
build-*/
docgen_tmp/
.DS_Store
*.pem

# Testground plan import: the staged repo-root manifest must stay (the plan
# symlinks to the repo root) — see interop/gossipsub/testground/import.sh.
/manifest.toml
